@~/.claude/prompts/new_functionality_prompt_spec.md

# Document AI-Assisted Changes vs. Human Review

## Role
Act as a Software Developer who practices transparent AI-assisted development and documents the review process critically.

## Context
Project: API REST de Gestión de Tareas — Spring Boot 3.2.5 / Java 17  
Location: `D:\Master-IA-Dev\04-Bloque4\1-4-10-api-spring-boot-tareas`  
Session retrospective: `RETROSPECTIVA-2026-04-20.md` documents the initial build session

**Non-compliant item:** `dc_cambios_ia_documentados`  
The project was built with AI assistance (claude-sonnet-4-6) but there is no section in the README or `docs/` folder documenting what was AI-generated vs. what the developer reviewed, changed, or added. Evaluation requires explicit critical review documentation — not just acknowledgment that AI was used.

Evidence of AI use: The `RETROSPECTIVA-2026-04-20.md` exists documenting the session, but this is not in the README and doesn't describe specific changes made to AI-generated code.

## Task
Add a "## AI-Assisted Development" section to `README.md` that:
1. Acknowledges which files/components were AI-generated
2. Documents at least 2 specific changes, corrections, or validations made by the developer to AI output
3. Explains the review process used (running tests, reading code, etc.)
4. Is honest — if everything passed the first time, document that and explain why tests provide the validation safety net

### Documentation Guidelines
- Be specific about which AI model was used: `claude-sonnet-4-6`
- List files that were AI-generated: all `.java` files, `pom.xml`, `.gitlab-ci.yml`
- Document at least one thing the developer verified manually (e.g., confirmed 13/13 tests pass)
- Document any corrections made (e.g., if any test failed and was fixed, or if a configuration was adjusted)
- If no changes were needed, explain WHY the AI output was accepted (what validation gave confidence)
- Place section after "## Decisions" and before "## Getting Started"

## Output Format
1. Updated `README.md` with a new "## AI-Assisted Development" section

## Examples and Steps to Follow
1. Read `RETROSPECTIVA-2026-04-20.md` to extract facts about the session
2. Review all source files to identify any patterns that differ from default AI output
3. Write the section documenting the process honestly
4. Insert into README at the correct location

Example section:
```markdown
## AI-Assisted Development

This project was scaffolded with the assistance of **claude-sonnet-4-6** (Claude Code CLI).

### What was AI-generated
All Java source files (`TareaController`, `TareaService`, `TareaRepository`, `Tarea`, `ApiTareasApplication`), `pom.xml`, `application.properties`, `schema.sql`, `.gitlab-ci.yml`, and the 13 integration tests in `TareaIntegrationTest.java` were generated by the AI agent in a single session.

### Developer review and validation
1. **Test execution verification**: All 13 integration tests were executed locally (`./mvnw test`) and confirmed passing before committing. The test output (`BUILD SUCCESS`, 0 failures) was verified directly in the terminal.
2. **GitLab CI configuration review**: The `.gitlab-ci.yml` was reviewed and the `-DskipTests` flag was noted as a limitation — tests are not run in CI. This is a known gap tracked in `docs/compliance/compliance_report.md`.
3. **Architecture review**: The layered architecture (Controller → Service → Repository) and constructor injection pattern were verified against Java/Spring best practices before acceptance.

### What was NOT changed
No corrections to generated code were required — the AI correctly implemented Bean Validation, JPA lifecycle callbacks, and the complete CRUD + filter logic. Confidence comes from the 13 integration tests that cover all endpoints and edge cases.
```

## Output Checklist and Guardrails
- [ ] AI model name explicitly mentioned (claude-sonnet-4-6)
- [ ] List of AI-generated files provided
- [ ] At least 2 specific review/validation actions documented
- [ ] Honest about gaps found (or honest about why none were found)
- [ ] No generic praise of AI ("it did a great job") — specific and factual only
- [ ] README section added without removing existing content
- [ ] Commit with descriptive message
